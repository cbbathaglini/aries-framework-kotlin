android.useAndroidX=true

# Mem�ria do Gradle Daemon (JVM do Gradle)
org.gradle.jvmargs=-Xms512m -Xmx3g -XX:MaxMetaspaceSize=512m -XX:+UseG1GC -XX:MaxGCPauseMillis=200 -Dfile.encoding=UTF-8

# Paralelismo e cache (melhoram performance e uso de mem�ria)
org.gradle.parallel=true
org.gradle.configureondemand=true
org.gradle.caching=true

# (Opcional) Limitar workers se a m�quina tem pouca RAM (ex.: 8 GB)
# org.gradle.workers.max=4

# (Opcional) Silenciar o warning espec�fico do daemon
# org.gradle.daemon.performance.disable-logging=true
VERSION_NAME=0.55.0-SNAPSHOT
GROUP=org.hyperledger